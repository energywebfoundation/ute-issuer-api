import { BlockchainProperties } from '@energyweb/issuer-api';

export * from './blockchain-properties.controller';
export * from './blockchain-properties.module';

export const entities = [BlockchainProperties];
