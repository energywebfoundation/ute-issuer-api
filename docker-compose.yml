version: "3.9"
services:
  issuer:
    build: .
    restart: always
    ports:
      - "3030:3030"
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ute-issuer
    ports:
      - "5432:5432"